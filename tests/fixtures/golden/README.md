# Golden Fixtures

These are recorded API responses used for validating MCP tool outputs.
Each file contains a request/response pair that represents expected behavior.

## Regenerating

Run `poetry run python tests/generate_golden.py` to regenerate these fixtures.
